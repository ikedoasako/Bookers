<h1>Books</h1>
<table>
  <thead>
    <tr>
      <th>title</th>
      <th>body</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= link_to 'show', book_path(book.id) %></td>
      <td><%= link_to 'edit', edit_book_path(book.id) %></td>
      <td><%= link_to 'destroy', book_path(book.id), method: :delete %></td>
      </tr>
      <tr>
        <td>トラブル知らずのシステム設計</td>
        <td>システム設計の要点を図解で説明してくれます</td>
        <td><%= link_to 'show', book_path(book.id) %></td>
        <td><%= link_to 'edit', edit_book_path(book.id) %></td>
        <td><%= link_to 'destroy', book_path(book.id), method: :delete %></td>
      </tr>
      <tr>
        <td>たのしいRuby</td>
        <td>Rubyの入門におすすめです</td>
        <td><%= link_to 'show', book_path(book.id) %></td>
        <td><%= link_to 'edit', edit_book_path(book.id) %></td>
        <td><%= link_to 'destroy', book_path(book.id), method: :delete %></td>
      </tr>
      <tr>
        <td>入門Git</td>
        <td>gitについての基本操作から内部構造まで解説してくれます</td>
        <td><%= link_to 'show', book_path(book.id) %></td>
        <td><%= link_to 'edit', edit_book_path(book.id) %></td>
        <td><%= link_to 'destroy', book_path(book.id), method: :delete %></td>
      </tr>
  　　　<tr>
  　　　　<td>アルゴリズム図鑑</td>
  　　　　<td>アルゴリズムについてカラーイラストでしっかり解説してくれます</td>
  　　　　<td><%= link_to 'show', book_path(book.id) %></td>
  　　　　<td><%= link_to 'edit', edit_book_path(book.id) %></td>
  　　　　<td><%= link_to 'destroy', book_path(book.id), method: :delete %></dr>
      </tr>
  <% end %>
  </tbody>
</table>

<h2>New Book</h2>
<%= form_with model: Book.new do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>
